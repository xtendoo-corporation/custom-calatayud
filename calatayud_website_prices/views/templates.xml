<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="product_custom" name="Custom Product View" inherit_id="website_sale.product">
        <xpath expr="//div[@id='oe_structure_website_sale_product_1']" position="inside">
            <t t-set="is_logout_user" t-value="request.env.user == request.website.user_id"/>
            <t t-set="is_log_user" t-value="True"/>
        </xpath>

        <!--Boton del carro-->
        <!--        <xpath expr="//div[@id='add_to_cart_wrap']" position="replace">-->
        <!--            <t t-if="is_log_user">-->
        <!--                <div id="add_to_cart_wrap"-->
        <!--                     t-attf-class="{{'d-none' if combination_info['prevent_zero_price_sale'] else 'd-inline-flex'}} align-items-center mb-2 me-auto">-->
        <!--                    <a data-animation-selector=".o_wsale_product_images" role="button" id="add_to_cart"-->
        <!--                       t-attf-class="btn btn-primary js_check_product a-submit flex-grow-1 #{'btn-lg' if ctaSizeBig else ''}"-->
        <!--                       href="#">-->
        <!--                        <i class="fa fa-shopping-cart me-2"/>-->
        <!--                        <span style="font-weight: bold">Añadir al carro</span>-->
        <!--                    </a>-->
        <!--                </div>-->
        <!--            </t>-->
        <!--        </xpath>-->

    </template>
   <template id="product_share_buttons" inherit_id="website_sale.product" active="True" name="Share Buttons" priority="22">
    <xpath expr="//div[@id='o_product_terms_and_share']" position="replace">
        <!-- No poner nada aquí lo elimina completamente -->
    </xpath>
</template>
     <template id="hide_default_documents" inherit_id="website_sale.product" priority="90">
        <!-- Oculta el bloque entero que genera la sección de Documentos -->
        <xpath expr="//div[contains(@class, 'o_wsale_product_documents') or @id='product_documents']" position="replace">
            <!-- removed -->
        </xpath>
    </template>
    <template id="product_quantity_inherit" inherit_id="website_sale.product_quantity">
        <xpath expr="//div[@id='o_wsale_cta_wrapper']" position="replace">
            <div id="o_wsale_cta_wrapper" class="d-flex flex-wrap align-items-center">
                <t t-set="hasQuantities" t-value="true"/>
                <t t-set="hasBuyNow" t-value="false"/>
                <t t-set="ctaSizeBig" t-value="not hasQuantities or not hasBuyNow"/>

                <t>
                    <div
                        t-attf-class="css_quantity input-group {{'d-none' if combination_info['prevent_zero_price_sale'] else 'd-inline-flex'}} me-2 mb-2 align-middle #{'input-group-lg' if ctaSizeBig else ''}"
                        contenteditable="false">
                        <a t-attf-href="#" class="btn btn-link js_add_cart_json" aria-label="Remove one"
                           title="Remove one">
                            <i class="fa fa-minus"></i>
                        </a>
                        <input type="text" class="form-control quantity text-center" data-min="1" name="add_qty"
                               t-att-value="add_qty or 1"/>
                        <a t-attf-href="#" class="btn btn-link float_left js_add_cart_json" aria-label="Add one"
                           title="Add one">
                            <i class="fa fa-plus"></i>
                        </a>
                    </div>

                    <div id="add_to_cart_wrap"
                         t-attf-class="{{'d-none' if combination_info['prevent_zero_price_sale'] else 'd-inline-flex'}} align-items-center mb-2 me-auto">
                        <a data-animation-selector=".o_wsale_product_images" role="button" id="add_to_cart"
                           t-attf-class="btn btn-primary js_check_product a-submit flex-grow-1 #{'btn-lg' if ctaSizeBig else ''}"
                           href="#">
                            <i class="fa fa-shopping-cart me-2"/>
                            <span style="font-weight: bold">Añadir al carrito</span>
                        </a>
                    </div>
                    <div id="product_option_block" class="d-flex flex-wrap w-100"/>
                </t>
            </div>
        </xpath>
    </template>

    <template id="custom_product_price" name="Custom Product Price" inherit_id="website_sale.product_price">
        <xpath expr="//div[@itemprop='offers']" position="inside">
            <t t-set="is_logout_user" t-value="request.env.user == request.website.user_id"/>
            <t t-set="is_log_user"
               t-value="request.env.user.has_group('base.group_user') or request.env.user.has_group('base.group_portal')"/>
        </xpath>

        <xpath expr="//h3[@class='css_editable_mode_hidden']" position="replace">
            <t>
                <h3 class="css_editable_mode_hidden">
                    <span class="oe_price"
                          t-attf-style="white-space: nowrap;"
                          t-esc="combination_info['price']"
                          t-options="{'widget': 'monetary', 'display_currency': website.currency_id}"/>
                    <span itemprop="price" style="display:none;" t-esc="combination_info['price']"/>
                    <span itemprop="priceCurrency" style="display:none;" t-esc="website.currency_id.name"/>
                    <span
                        t-attf-class="text-danger oe_default_price ms-1 h5 {{'' if combination_info['has_discounted_price'] and not combination_info['compare_list_price'] else 'd-none'}}"
                        style="text-decoration: line-through; white-space: nowrap;"
                        t-esc="combination_info['list_price']"
                        t-options="{'widget': 'monetary', 'display_currency': website.currency_id}"
                        itemprop="listPrice"/>
                    <t t-if="is_view_active('website_sale.tax_indication')" t-call="website_sale.tax_indication"/>
                    <del
                        t-if="combination_info['compare_list_price'] and (combination_info['compare_list_price'] &gt; combination_info['price'])">
                        <bdi dir="inherit">
                            <span t-esc="combination_info['compare_list_price']"
                                  groups="website_sale.group_product_price_comparison"
                                  t-options="{
                              'widget': 'monetary',
                              'display_currency': website.currency_id,
                          }"/>
                        </bdi>
                    </del>
                </h3>
            </t>
        </xpath>

    </template>

    <!-- Intercambiar breadcrumb y barra de búsqueda -->
    <template id="swap_breadcrumb_search" name="Swap Breadcrumb and Search" inherit_id="website_sale.product" priority="20">
        <!-- Reemplazar toda la fila que contiene breadcrumb y búsqueda -->
        <xpath expr="//section[@id='product_detail']/div[@class='row align-items-center']" position="replace">
            <div class="row align-items-center">
                <!-- Columna izquierda: Barra de búsqueda -->
                <div class="col-lg-4 d-flex align-items-center order-2 order-lg-0">
                    <div class="d-flex justify-content-between w-100">
                        <t t-if="is_view_active('website_sale.search')" t-call="website_sale.search">
                            <t t-set="search" t-value="False"/>
                            <t t-set="_form_classes" t-valuef="mb-4 mb-lg-0"/>
                            <t t-set="_classes" t-value="'me-sm-2'"/>
                        </t>
                        <t t-call="website_sale.pricelist_list">
                            <t t-set="_classes" t-valuef="d-lg-inline ms-2"/>
                        </t>
                    </div>
                </div>
                <!-- Columna derecha: Breadcrumb -->
                <div class="col d-flex align-items-center justify-content-end order-1 order-lg-1">
                    <ol class="o_wsale_breadcrumb breadcrumb p-0 mb-4 m-lg-0">
                        <li class="o_not_editable breadcrumb-item d-none d-lg-inline-block">
                            <a t-att-href="keep(category=0, attribute_value=0, tags=0)">
                                <i class="oi oi-chevron-left d-lg-none me-1" role="presentation"/>Productos
                            </a>
                        </li>
                        <li
                            t-nocache="The category does not have to be cached, as the product can be accessed via different paths."
                            t-if="category"
                            class="breadcrumb-item"
                        >
                            <a
                                class="py-2 py-lg-0"
                                t-att-href="keep('/shop/category/%s' % slug(category), category=0, attribute_value=0, tags=0)"
                            >
                                <i class="oi oi-chevron-left d-lg-none me-1" role="presentation"/><t t-out="category.name"/>
                            </a>
                        </li>
                        <li t-else="" class="o_not_editable breadcrumb-item d-lg-none">
                            <a
                                class="py-2 py-lg-0"
                                t-att-href="keep(category=0, attribute_value=0, tags=0)"
                            >
                                <i class="oi oi-chevron-left me-1" role="presentation"/>Productos
                            </a>
                        </li>
                        <li class="breadcrumb-item d-none d-lg-inline-block active">
                            <span t-field="product.name" />
                        </li>
                    </ol>
                </div>
            </div>
        </xpath>
    </template>

    <!-- Reorganizar layout del producto: descripción debajo de imagen y categoría debajo del nombre -->
    <template id="product_layout_custom" name="Custom Product Layout" inherit_id="website_sale.product" priority="25">
        <!-- Mover la descripción del producto debajo del nombre -->
        <xpath expr="//div[@id='product_details']/h1" position="after">
            <!-- Agregar categoría o información adicional debajo del nombre -->
            <p class="text-muted mb-3" t-if="product.categ_id">
                <span t-field="product.categ_id.name"/>
            </p>
        </xpath>
</template>


    <!--    <template id="variants" inherit_id="website_sale_product_image_sample.variants">-->
</odoo>
