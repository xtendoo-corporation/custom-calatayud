<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

    <t t-name="website_sale_product_image_sample.ImageProductAttribute">
        <div class="configurator_radio" t-ref="root">
            <div class="d-flex flex-wrap gap-2">
                <t t-foreach="values" t-as="value" t-key="value.id">
                    <t t-set="imgUrl" t-value="getVariantImageUrl(value)"/>
                    <div class="attribute-name-cell" t-att-class="{'ptav-not-available': value.excluded}">
                        <input class="form-check-input d-none" type="radio" t-model="state.attribute_value_ids" t-att-name="value.attribute_id.id"
                            t-attf-id="{{ value.attribute_id.id }}_{{ value.id }}" t-att-value="value.id"/>
                        <label
                            t-attf-class="btn btn-secondary btn-lg lh-lg d-flex flex-column align-items-center gap-1 website_sale_product_image_sample_swatch {{ value.id == state.attribute_value_ids ? 'active' : '' }}"
                            t-att-name="value.name"
                            t-attf-for="{{ value.attribute_id.id }}_{{ value.id }}">
                            <img t-if="imgUrl" t-att-src="imgUrl" class="website_sale_product_image_sample_thumb"/>
                            <span t-esc="value.name"/>
                            <div t-if="value.price_extra" class="price-extra-cell d-inline-block">
                                <span class="price_extra px-2 py-1 rounded-pill text-bg-info">
                                    <t t-esc="getFormatPriceExtra(value.price_extra)"/>
                                </span>
                            </div>
                        </label>
                    </div>
                </t>
            </div>
        </div>
    </t>

    <t t-name="website_sale_product_image_sample.ProductConfiguratorPopup" t-inherit="point_of_sale.ProductConfiguratorPopup" t-inherit-mode="extension">
        <xpath expr="//ColorProductAttribute" position="after">
            <ImageProductAttribute t-elif="attributeLine.attribute_id.display_type === 'image'" attributeLine="attributeLine" defaultValues="this.props.defaultValues"/>
        </xpath>
    </t>

</templates>
