<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Vista heredada para hacer visibles los campos de Tax Excluded y Tax Included -->
        <record id="view_move_form_tax_fields_visible" model="ir.ui.view">
            <field name="name">account.move.form.tax.fields.visible</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">

                <!-- Hacer visible el campo Tax Excluded (Subtotal) -->
                <xpath expr="//field[@name='invoice_line_ids']//tree//field[@name='price_subtotal']" position="attributes">
                    <attribute name="optional">show</attribute>
                    <attribute name="groups"></attribute>
                </xpath>

                <!-- Hacer visible el campo Tax Included (Total) -->
                <xpath expr="//field[@name='invoice_line_ids']//tree//field[@name='price_total']" position="attributes">
                    <attribute name="optional">show</attribute>
                    <attribute name="groups"></attribute>
                </xpath>

            </field>
        </record>
    </data>
</odoo>
